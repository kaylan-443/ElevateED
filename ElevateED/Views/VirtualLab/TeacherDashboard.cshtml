@model ElevateED.ViewModels.TeacherLabDashboardViewModel
@using ElevateED.Models

@{
    ViewBag.Title = "Virtual Lab Management - Teacher";
    Layout = "~/Views/Shared/_TeacherLayout.cshtml";
}

<style>
    /* MAIN CONTENT AREA - Light background */
    .content-area {
        background: #f8f9fa;
        min-height: 100vh;
        padding: 30px;
    }

    /* Page Header */
    .page-header {
        background: white;
        border-radius: 16px;
        padding: 20px 25px;
        margin-bottom: 25px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        border: 1px solid #e5e7eb;
    }

        .page-header h1 {
            font-size: 24px;
            font-weight: 700;
            color: #1e3c72;
            margin: 0;
        }

        .page-header p {
            color: #6b7280;
            margin: 5px 0 0 0;
        }

    /* Stats Grid */
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
    }

    .stat-card {
        background: white;
        border-radius: 20px;
        padding: 25px 20px;
        text-align: center;
        border: 1px solid #e5e7eb;
        box-shadow: 0 2px 8px rgba(0,0,0,0.04);
        transition: all 0.3s;
    }

        .stat-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }

    .stat-number {
        font-size: 36px;
        font-weight: 800;
        color: #4f46e5;
        margin-bottom: 5px;
    }

        .stat-number.pending {
            color: #f59e0b;
        }

    .stat-lbl {
        font-size: 13px;
        color: #6b7280;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        font-weight: 600;
    }

    /* Section Titles */
    .section-title {
        font-size: 18px;
        font-weight: 700;
        color: #1e3c72;
        margin: 30px 0 20px 0;
        padding-bottom: 12px;
        border-bottom: 3px solid #d4af37;
        display: flex;
        align-items: center;
        gap: 10px;
    }

        .section-title i {
            color: #d4af37;
            font-size: 20px;
        }

    /* Lab Cards */
    .lab-card {
        background: white;
        border-radius: 16px;
        padding: 22px;
        margin-bottom: 18px;
        border: 1px solid #e5e7eb;
        box-shadow: 0 2px 6px rgba(0,0,0,0.04);
        transition: all 0.3s;
    }

        .lab-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px rgba(0,0,0,0.1);
        }

        .lab-card.pending-card {
            border-left: 5px solid #f59e0b;
            background: #fffbeb;
        }

    .lab-title {
        font-size: 18px;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 10px;
    }

    .lab-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        margin-bottom: 12px;
    }

    /* Badges - HIGH CONTRAST */
    .badge-lab {
        display: inline-block;
        padding: 5px 14px;
        border-radius: 20px;
        font-size: 11px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .badge-biology {
        background: #10b981;
        color: white;
        box-shadow: 0 1px 2px rgba(0,0,0,0.1);
    }

    .badge-chemistry {
        background: #f59e0b;
        color: white;
        box-shadow: 0 1px 2px rgba(0,0,0,0.1);
    }

    .badge-physics {
        background: #3b82f6;
        color: white;
        box-shadow: 0 1px 2px rgba(0,0,0,0.1);
    }

    .badge-difficulty {
        background: #e2e8f0;
        color: #1e293b;
        border: 1px solid #cbd5e1;
    }

    .lab-card p {
        color: #475569;
        font-size: 14px;
        margin: 10px 0;
        line-height: 1.5;
    }

    /* BUTTONS - HIGH VISIBILITY */
    .btn-launch {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 10px 22px;
        border-radius: 30px;
        font-size: 13px;
        font-weight: 600;
        cursor: pointer;
        text-decoration: none;
        transition: all 0.2s;
        border: none;
        margin-right: 10px;
        margin-top: 8px;
    }

        .btn-launch i {
            font-size: 14px;
        }

        .btn-launch:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 14px rgba(0,0,0,0.15);
            text-decoration: none;
        }

    .btn-primary-custom {
        background: #4f46e5;
        color: white !important;
    }

        .btn-primary-custom:hover {
            background: #4338ca;
            color: white !important;
        }

    .btn-success-custom {
        background: #10b981;
        color: white !important;
    }

        .btn-success-custom:hover {
            background: #059669;
            color: white !important;
        }

    .btn-warning-custom {
        background: #f59e0b;
        color: #1e1b4b !important;
    }

        .btn-warning-custom:hover {
            background: #d97706;
            color: white !important;
        }

    .btn-info-custom {
        background: #3b82f6;
        color: white !important;
    }

        .btn-info-custom:hover {
            background: #2563eb;
            color: white !important;
        }

    .btn-outline-custom {
        background: white;
        color: #4f46e5 !important;
        border: 2px solid #4f46e5;
    }

        .btn-outline-custom:hover {
            background: #4f46e5;
            color: white !important;
        }

    /* Assignment Card */
    .assignment-card {
        background: #f8fafc;
        border-radius: 14px;
        padding: 18px;
        margin-bottom: 14px;
        border: 1px solid #e2e8f0;
    }

    .assignment-title {
        font-weight: 700;
        color: #1e3c72;
        margin-bottom: 8px;
    }

    .assignment-meta {
        font-size: 13px;
        color: #64748b;
        display: flex;
        gap: 20px;
        flex-wrap: wrap;
        margin-bottom: 12px;
    }

    /* Empty State */
    .empty-state {
        text-align: center;
        padding: 60px 20px;
        background: white;
        border-radius: 20px;
        border: 2px dashed #cbd5e1;
    }

        .empty-state i {
            font-size: 56px;
            color: #94a3b8;
            margin-bottom: 15px;
        }

        .empty-state h4 {
            color: #475569;
            margin-bottom: 8px;
        }

        .empty-state p {
            color: #94a3b8;
        }

    /* Create Button */
    .btn-create {
        background: linear-gradient(135deg, #10b981, #059669);
        color: white;
        padding: 12px 28px;
        border-radius: 40px;
        font-weight: 700;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        transition: all 0.3s;
        border: none;
        font-size: 14px;
    }

        .btn-create:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(16, 185, 129, 0.3);
            color: white;
            text-decoration: none;
        }

    /* Warning note for manual assignment */
    .assignment-note {
        background: #fef3c7;
        border-left: 4px solid #f59e0b;
        padding: 12px 18px;
        border-radius: 10px;
        margin-bottom: 20px;
        font-size: 13px;
        color: #92400e;
    }

        .assignment-note i {
            margin-right: 8px;
            color: #f59e0b;
        }

        .assignment-note a {
            color: #d97706;
            font-weight: 600;
            text-decoration: none;
        }

            .assignment-note a:hover {
                text-decoration: underline;
            }
</style>

<div class="content-area">
    <div class="page-header">
        <div>
            <h1><i class="fas fa-vr-cardboard" style="color: #8b5cf6;"></i> Virtual Lab Management</h1>
            <p>Create interactive 3D experiments, assign to classes, and grade student submissions</p>
        </div>
        <a href="@Url.Action("CreateExperiment", "VirtualLab")" class="btn-create">
            <i class="fas fa-plus-circle"></i> Create New Experiment
        </a>
    </div>

    <!-- Statistics Dashboard -->
    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-number">@Model.Experiments.Count</div>
            <div class="stat-lbl">Total Experiments</div>
        </div>
        <div class="stat-card">
            <div class="stat-number">@Model.Assignments.Count</div>
            <div class="stat-lbl">Active Assignments</div>
        </div>
        <div class="stat-card">
            <div class="stat-number pending">@Model.PendingSubmissions.Count</div>
            <div class="stat-lbl">Pending Grading</div>
        </div>
    </div>

    <!-- Pending Submissions Section (Most Urgent) -->
    @if (Model.PendingSubmissions.Any())
    {
        <div class="section-title">
            <i class="fas fa-clock"></i> Pending Submissions
        </div>
        foreach (var sub in Model.PendingSubmissions)
        {
            <div class="lab-card pending-card">
                <div class="lab-title">
                    <i class="fas fa-flask" style="color: #f59e0b; margin-right: 8px;"></i>
                    @sub.Assignment.Experiment.Title
                </div>
                <div class="lab-meta">
                    <span class="badge-lab badge-@sub.Assignment.Experiment.LabType.ToString().ToLower()">@sub.Assignment.Experiment.LabType Lab</span>
                    <span><i class="fas fa-user"></i> Student: @sub.Student?.FullName</span>
                    <span><i class="fas fa-calendar-check"></i> Submitted: @sub.CompletedAt?.ToString("dd MMM yyyy, HH:mm")</span>
                </div>
                <p><strong>Observations:</strong> @(sub.Observations?.Length > 100 ? sub.Observations.Substring(0, 100) + "..." : sub.Observations ?? "No observations recorded")</p>
                <div>
                    <a href="@Url.Action("GradeSubmissions", "VirtualLab", new { experimentId = sub.Assignment.ExperimentId })" class="btn-launch btn-warning-custom">
                        <i class="fas fa-check-double"></i> Grade Submission
                    </a>
                </div>
            </div>
        }
    }

    <!-- Active Assignments Section -->
    @if (Model.Assignments.Any())
    {
        <div class="section-title">
            <i class="fas fa-tasks"></i> Assigned Experiments
        </div>
        foreach (var assignment in Model.Assignments)
        {
            <div class="assignment-card">
                <div class="assignment-title">
                    <i class="fas fa-flask" style="color: #10b981;"></i> @assignment.Experiment.Title
                </div>
                <div class="assignment-meta">
                    <span><i class="fas fa-users"></i> Class: @assignment.Class?.FullName</span>
                    <span><i class="far fa-calendar-alt"></i> Assigned: @assignment.AssignedAt.ToString("dd MMM yyyy")</span>
                    <span><i class="fas fa-hourglass-half"></i> Due: @assignment.DueDate.ToString("dd MMM yyyy")</span>
                </div>
                <div>
                    <a href="@Url.Action("GradeSubmissions", "VirtualLab", new { experimentId = assignment.ExperimentId })" class="btn-launch btn-outline-custom">
                        <i class="fas fa-chart-line"></i> View Submissions
                    </a>
                </div>
            </div>
        }
    }

    <!-- All Experiments List -->
    <div class="section-title">
        <i class="fas fa-database"></i> Experiment Library
    </div>

    <!-- Note about manual assignment -->
    <div class="assignment-note">
        <i class="fas fa-info-circle"></i>
        <strong>Assigning Experiments:</strong> To assign an experiment to a class, please go to the experiment details page via the "Create New Experiment" button above, or use the "Grade Submissions" button to access and assign experiments.
    </div>

    @if (!Model.Experiments.Any())
    {
        <div class="empty-state">
            <i class="fas fa-flask"></i>
            <h4>No Experiments Created Yet</h4>
            <p>Get started by creating your first virtual lab experiment.</p>
            <a href="@Url.Action("CreateExperiment", "VirtualLab")" class="btn-create" style="margin-top: 15px;">
                <i class="fas fa-plus-circle"></i> Create First Experiment
            </a>
        </div>
    }
    else
    {
        foreach (var exp in Model.Experiments)
        {
            <div class="lab-card">
                <div class="lab-title">
                    @exp.Title
                </div>
                <div class="lab-meta">
                    <span class="badge-lab badge-@exp.LabType.ToString().ToLower()">@exp.LabType Lab</span>
                    <span class="badge-lab badge-difficulty">@exp.Difficulty</span>
                    <span><i class="fas fa-star"></i> Grade @exp.GradeLevel</span>
                    <span><i class="fas fa-clock"></i> @exp.DurationMinutes mins</span>
                </div>
                <p>@exp.Description</p>
                <div>
                    <!-- Assign button REMOVED - only Grade Submissions remains -->
                    <a href="@Url.Action("GradeSubmissions", "VirtualLab", new { experimentId = exp.Id })" class="btn-launch btn-warning-custom">
                        <i class="fas fa-graduation-cap"></i> Grade Submissions
                    </a>
                </div>
            </div>
        }
    }
</div>

<script>
    // Simple dropdown toggle function if needed
    function toggleDropdown() {
        var dropdown = document.getElementById('profileDropdown');
        if (dropdown) dropdown.classList.toggle('show');
    }

    document.addEventListener('click', function (e) {
        var container = document.querySelector('.profile-container');
        var dropdown = document.getElementById('profileDropdown');
        if (container && dropdown && !container.contains(e.target)) {
            dropdown.classList.remove('show');
        }
    });
</script>